{
  "name": "pollinations-mcp-server",
  "version": "1.0.0",
  "description": "Free MCP server that generates images via Pollinations.ai (no API key required)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
